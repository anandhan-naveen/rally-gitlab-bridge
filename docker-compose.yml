services:
  bridge:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - bridge_data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/bridge.db
volumes:
  bridge_data:

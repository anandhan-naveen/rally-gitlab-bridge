services:
  bridge:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - bridge_data:/app/data
    environment:
      DATABASE_FILE: /app/data/bridge.db
      RALLY_SNAPSHOT_FILE: /app/data/rally_snapshot.json
volumes:
  bridge_data:
